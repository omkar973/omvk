# omvk
## Project Overview
This project demonstrates how to manage HTML and CSS development in separate branches and merge them into the main branch.
- The main branch contains the core HTML structure
- The frontend branch contains the CSS styling
- CSS changes are merged into the main branch after development

## 📂 Project Structure

```bash
project-folder/
│
├── index.html
├── styles.css
└── README.md
